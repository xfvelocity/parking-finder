export enum AUTH_TYPE {
  LOGIN = "login",
  REGISTER = "register",
  EMAIL_SENT = "email-sent",
  EMAIL_INVALID = "email-invalid",
}

export enum INFO_TYPE {
  VIEWING = "viewing",
  PRICE = "price",
  INFO = "info",
  TIMES = "times",
  SUBMIT = "submit",
}
